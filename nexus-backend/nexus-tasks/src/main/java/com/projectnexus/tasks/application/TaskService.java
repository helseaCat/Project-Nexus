package com.projectnexus.tasks.application;

/**
 * Application service for Task management and Kanban workflows.
 */
public interface TaskService {

    // TODO: Define use cases
    // - createTask(CreateTaskCommand)
    // - updateTaskStatus(UUID taskId, TaskStatus newStatus)
    // - assignTask(UUID taskId, UUID assigneeId)
    // - addComment(UUID taskId, AddCommentCommand)
    // - listTasks(UUID tenantId, Pageable)
    // - getTasksByLink(LinkType, UUID linkedToId)
}
