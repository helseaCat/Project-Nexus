package com.projectnexus.contracts.application;

/**
 * Application service / use case orchestrator for Data Contracts.
 * Coordinates domain logic, persistence, and events.
 */
public interface DataContractService {

    // TODO: Define use cases
    // - createContract(CreateContractCommand)
    // - publishContract(UUID contractId)
    // - addTestVariable(UUID contractId, AddVariableCommand)
    // - getContract(UUID contractId)
    // - listContracts(UUID tenantId, Pageable)
}
