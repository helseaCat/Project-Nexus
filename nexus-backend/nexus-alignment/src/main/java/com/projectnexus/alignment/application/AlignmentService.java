package com.projectnexus.alignment.application;

/**
 * Application service for Alignment Expectations and deviation monitoring.
 */
public interface AlignmentService {

    // TODO: Define use cases
    // - createExpectation(CreateExpectationCommand)
    // - listExpectations(UUID contractId)
    // - evaluatePayload(UUID payloadId) → List<Deviation>
}
